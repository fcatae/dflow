{
  "name": "umaflow",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run build && node out/index",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fabricio Catae",
  "devDependencies": {
    "@types/node": "^10.3.4",
    "typescript": "^2.9.2"
  }
}
